import CarCards from './CarCards';
import React, { Component } from 'react';

class Main extends Component {
    render() {
        return (
            <div>
                <CarCards />
            </div>
        )
    }
}

export default Main;